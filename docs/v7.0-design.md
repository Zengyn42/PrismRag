# PrismRag v7.0 联邦元图 技术设计文档

> 版本：v7.0-design-draft  
> 日期：2026-05-26  
> 状态：设计定稿，待实现  
> 前置版本：v5.7（已完成）

---

## 目录

1. [概览与架构图](#1-概览与架构图)
2. [模块设计](#2-模块设计)
   - 2.1 [manifest.json 写入（ingest 侧）](#21-manifestjson-写入ingest-侧)
   - 2.2 [registry_cache.py（新建）](#22-registry_cachepy新建)
   - 2.3 [serve 自动发现（server.py 改动）](#23-serve-自动发现serverpy-改动)
   - 2.4 [三层 Bridge（federated.py 改动）](#24-三层-bridgefederatedpy-改动)
   - 2.5 [写操作热补丁（server.py 改动）](#25-写操作热补丁serverpy-改动)
   - 2.6 [联邦元图可视化（visualize.py 改动）](#26-联邦元图可视化visualizepy-改动)
3. [数据 Schema](#3-数据-schema)
4. [关键流程序列图](#4-关键流程序列图)
5. [错误处理与降级策略](#5-错误处理与降级策略)
6. [迁移路径（v5.7 → v7.0）](#6-迁移路径v57--v60)
7. [实现顺序 P0 → P3](#7-实现顺序-p0--p3)
8. [测试矩阵](#8-测试矩阵)
9. [架构决策记录（ADR）— 2026-06-10 辩论定案](#9-架构决策记录adr--2026-06-10-辩论定案)

---

## 1. 概览与架构图

### v7.0 在 v5.7 基础上增加了什么

v5.7 已具备联邦多图查询能力，但存在以下核心痛点：

| 痛点 | v5.7 现状 | v7.0 解决方案 |
|------|----------|--------------|
| 图注册依赖手工配置 | `PRISM_GRAPHS` 环境变量手动填写 | `registry_cache.json` 自动发现，ingest 后自动注册 |
| Embedding bridge 复杂度过高 | `_build_embedding_bridges`：O(N×M)全量向量对比，加载时间随节点数线性增长 | 三层 bridge：O(50²) Hub 代表点，骨架一次建好，细粒度按需 lazy |
| 新节点写入后查询不可见 | write_note 只写磁盘，内存图不更新 | 热补丁立即注入内存图，BM25/Lance 异步重建 |
| 缺乏全局视图 | 无跨 namespace 可视化 | 元图 HTML：namespace 超级节点 + bridge 虚线 + portal 链接 |
| hub/社区信息无处存放 | 没有 manifest，没有 registry | manifest 存 source of truth；registry_cache 存派生数据（hub_node_ids、版本号） |

### 整体架构（ASCII）

```
  ┌─────────────────────────────────────────────────────────────────┐
  │                         Vault / Code Repo                        │
  │   (per-namespace, physically isolated)                           │
  │                                                                   │
  │  .prismrag/<ns>/                                                  │
  │    ├── graph.json          ← ingest 输出                         │
  │    ├── lance/              ← embedding 向量库                    │
  │    ├── bm25_cache/         ← BM25 序列化缓存                     │
  │    └── manifest.json  ◄──── NEW v7.0: ingest 完成后写入          │
  └─────────────────────┬───────────────────────────────────────────┘
                         │ register()
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │          ~/.prismrag/registry_cache.json   NEW v7.0              │
  │  {entries: {ns: {manifest_path, hub_node_ids, hub_set_version}}} │
  └─────────────────────┬───────────────────────────────────────────┘
                         │ discover_all()
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │              prism-rag serve (MCP Server)                        │
  │                                                                   │
  │   _ensure_federated()                                             │
  │     ├── discover_all() ─── NEW v7.0 (取代 PRISM_GRAPHS)         │
  │     ├── FederatedGraph.load(sources)                             │
  │     └── build_bridges()                                          │
  │           ├── Layer-1: shared_tag + import  (确定性，零成本)     │
  │           ├── Layer-2: hub ANN bridge        (O(50²), <1s)  NEW  │
  │           └── Layer-3: lazy query-time ANN   (LRU 缓存)     NEW  │
  │                                                                   │
  │   MCP Tools (18 tools)                                           │
  │     ├── search_knowledge / explain_node / trace_path / ...       │
  │     │     └── scope="" | "ns1" | "ns1,ns2"                       │
  │     ├── write_note / patch_note                                   │
  │     │     └── hot_patch() → 内存注入 + 异步 rebuild  NEW        │
  │     └── list_namespaces / cross_graph_bridges            NEW      │
  └─────────────────────┬───────────────────────────────────────────┘
                         │ HTML output
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │              可视化层  NEW v7.0                                   │
  │   ~/.prismrag/meta_graph.html  ← namespace 超级节点元图          │
  │   .prismrag/<ns>/graph.html    ← 单 namespace（已有）            │
  │   /tmp/prismrag_query_*.html   ← 跨图查询结果动态 HTML          │
  └─────────────────────────────────────────────────────────────────┘
```

---

## 2. 模块设计

### 2.1 manifest.json 写入（ingest 侧）

**改动文件**：`prism_rag/cli.py`

ingest 完成后（`prism-rag ingest`、`prism-rag ingest-project`、`ingest-code`）在输出目录写入 `manifest.json`，并向 registry_cache 注册。

#### 函数签名

```python
from datetime import datetime, timezone
from pathlib import Path
from typing import Any
import json

def _write_manifest(
    out_dir: Path,
    namespace: str,
    vault_path: Path,
    graph: "KnowledgeGraph",
    settings: "PrismRagSettings",
) -> Path:
    """Write manifest.json to out_dir after ingest completes.

    Args:
        out_dir:     .prismrag/<ns>/ 目录（data_dir）
        namespace:   namespace 字符串，如 "pulsify"
        vault_path:  被 ingest 的 vault 或 repo 根目录
        graph:       已加载完成的 KnowledgeGraph 实例
        settings:    PrismRagSettings，用于读取模型信息

    Returns:
        写入的 manifest.json 路径
    """

def _register_manifest(manifest_path: Path, namespace: str) -> None:
    """Register manifest_path into ~/.prismrag/registry_cache.json.
    Silently logs on failure (non-blocking).
    """
```

#### 关键逻辑伪代码

```python
def _write_manifest(out_dir, namespace, vault_path, graph, settings):
    embed_label = f"{settings.embed_backend}/{settings.ollama_model}"
    manifest = {
        "schema_version": 1,
        "namespace": namespace,
        "vault_path": str(vault_path.resolve()),
        "data_dir": str(out_dir.resolve()),
        "ingest_time": datetime.now(timezone.utc).isoformat().replace("+00:00", "Z"),
        "node_count": graph.node_count,
        "edge_count": graph.edge_count,
        "embedding_model": embed_label,
        "embedding_dim": settings.embedding_dim,
        "prismrag_version": __version__,
    }
    manifest_path = out_dir / "manifest.json"
    # 原子写：写 tmp 文件再 os.replace
    tmp = manifest_path.with_suffix(".json.tmp")
    tmp.write_text(json.dumps(manifest, indent=2, ensure_ascii=False), encoding="utf-8")
    os.replace(tmp, manifest_path)

    # 注册到 registry_cache（失败不阻断 ingest）
    try:
        _register_manifest(manifest_path, namespace)
    except Exception as exc:
        logger.warning(f"[ingest] registry_cache 注册失败（非致命）: {exc}")

    return manifest_path
```

#### 与现有代码接口

- 在 `cli.py` 的各 `ingest_*` 函数末尾调用 `_write_manifest()`，位置在 `graph.save()` 之后。
- `KnowledgeGraph.node_count` / `edge_count` 已有，直接使用。
- `settings.embedding_dim` 已有，`settings.ollama_model` 已有。

---

### 2.2 registry_cache.py（新建）

**新建文件**：`prism_rag/store/registry_cache.py`

负责全局 namespace 注册表的读写，是 serve 自动发现的数据来源。

#### 完整类/函数签名

```python
"""Global namespace registry cache.

Stores per-namespace derived metadata (hub nodes, versions) keyed by namespace.
Written with filelock + atomic rename; read is lock-free.

Location: ~/.prismrag/registry_cache.json
"""
from __future__ import annotations

import json
import logging
import os
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

logger = logging.getLogger(__name__)

CACHE_PATH: Path = Path.home() / ".prismrag" / "registry_cache.json"
_SCHEMA_VERSION: int = 1


class RegistryEntry:
    """Typed view of a single registry_cache entry."""

    manifest_path: str           # 绝对路径字符串
    last_seen: str               # ISO-8601 UTC timestamp
    hub_node_ids: list[str]      # 代表点 node IDs（含 ns:: 前缀）
    hub_set_version: int         # Hub 集版本号，每次重建 +1
    community_count: int         # 社区数量（Leiden 结果）


def register(
    manifest_path: Path,
    namespace: str,
) -> None:
    """Upsert namespace entry. Idempotent; safe for concurrent calls.

    Uses filelock (portalocker / filelock library) + atomic rename.
    Only updates manifest_path and last_seen; never overwrites hub_* fields
    if they already exist (to avoid clobbering P1 data from P0 call).
    """

def update_hub(
    namespace: str,
    hub_node_ids: list[str],
    hub_set_version: int,
    community_count: int,
) -> None:
    """Write hub representative node IDs and version after Layer-2 rebuild.
    Called by FederatedGraph._rebuild_hubs().
    """

def discover_all(
    cache_path: Path = CACHE_PATH,
) -> list[dict[str, Any]]:
    """Load cache, stat-probe each manifest_path, evict ghosts.

    Returns list of live entries (manifest_path exists on disk).
    Ghost entries (manifest_path missing) are evicted and cache rewritten.
    Stat probe costs < 1ms per entry on local FS.
    """

def get_manifest(namespace: str, cache_path: Path = CACHE_PATH) -> dict | None:
    """Parse and return a namespace's manifest.json. Returns None on error."""

def get_hub_version(namespace: str, cache_path: Path = CACHE_PATH) -> int:
    """Return hub_set_version for namespace, or 0 if not found."""

def _atomic_write(path: Path, data: dict) -> None:
    """Write dict as JSON to path using tmp-file + os.replace pattern."""

def _load_cache(path: Path) -> dict:
    """Load and validate cache JSON. Returns empty schema on missing/corrupt file."""
```

#### 核心实现伪代码

```python
def register(manifest_path, namespace):
    from filelock import FileLock
    lock_path = CACHE_PATH.with_suffix(".json.lock")
    CACHE_PATH.parent.mkdir(parents=True, exist_ok=True)

    with FileLock(str(lock_path), timeout=5):
        cache = _load_cache(CACHE_PATH)
        entries = cache.setdefault("entries", {})
        existing = entries.get(namespace, {})

        entries[namespace] = {
            **existing,                               # 保留已有 hub_* 字段
            "manifest_path": str(manifest_path.resolve()),
            "last_seen": _utcnow_iso(),
        }
        # 若是新条目，初始化 hub 字段
        if "hub_node_ids" not in entries[namespace]:
            entries[namespace].update({
                "hub_node_ids": [],
                "hub_set_version": 0,
                "community_count": 0,
            })
        _atomic_write(CACHE_PATH, cache)


def discover_all(cache_path=CACHE_PATH):
    if not cache_path.exists():
        return []
    cache = _load_cache(cache_path)
    entries = cache.get("entries", {})
    live = {}
    evicted = []

    for ns, entry in entries.items():
        mp = Path(entry.get("manifest_path", ""))
        if mp.exists():                               # stat 探活
            live[ns] = entry
        else:
            evicted.append(ns)
            logger.debug(f"[registry_cache] evict ghost: {ns} ({mp})")

    if evicted:
        cache["entries"] = live
        try:
            _atomic_write(cache_path, cache)
        except Exception as exc:
            logger.warning(f"[registry_cache] 幽灵清理写入失败: {exc}")

    return [{"namespace": ns, **entry} for ns, entry in live.items()]
```

#### 依赖

新增 `pyproject.toml` 依赖：
```toml
[project.optional-dependencies]
federation = [
    "filelock>=3.12",
]
```

`filelock` 跨平台（Linux/macOS/WSL），规避 `fcntl.flock` 的 WSL 边界问题（在被否定方案清单中有记录）。

---

### 2.3 serve 自动发现（server.py 改动）

**改动文件**：`prism_rag/mcp_server/server.py`

修改 `_ensure_federated()` 的图加载逻辑，优先从 registry_cache 自动发现，PRISM_GRAPHS 作为向后兼容覆盖。

#### 改动后的 `_ensure_federated` 骨架

```python
def _ensure_federated() -> FederatedGraph:
    global _federated, _bm25_indices, _embedding_stores, _embedder, _cross_ns_probe
    if _federated is not None:
        return _federated

    settings = PrismRagSettings()

    # -- 自动发现 vs 手动配置 ----------------------------------------
    if settings.resolved_graphs:
        # PRISM_GRAPHS 显式配置：向后兼容路径（v5.7 行为不变）
        sources = settings.resolved_graphs
        logger.info("[mcp] using PRISM_GRAPHS explicit config")
    else:
        # v7.0 新路径：从 registry_cache 自动发现
        from prism_rag.store.registry_cache import discover_all, get_manifest
        from prism_rag.config import GraphSource
        entries = discover_all()
        sources = []
        for entry in entries:
            manifest = get_manifest(entry["namespace"])
            if manifest is None:
                continue
            sources.append(GraphSource(
                namespace=manifest["namespace"],
                vault_path=Path(manifest["vault_path"]),
                data_dir=Path(manifest["data_dir"]),
            ))
        logger.info(f"[mcp] auto-discovered {len(sources)} namespaces from registry_cache")

    if not sources:
        logger.warning("[mcp] no graph sources found; federated graph will be empty")

    # -- 构建联邦图（逻辑与 v5.7 相同）-------------------------------
    from prism_rag.store.cross_namespace_probe import CrossNamespaceProbe
    log_path = settings.data_dir / "cross_namespace_log.jsonl"
    _cross_ns_probe = CrossNamespaceProbe(log_path=log_path)

    _federated = FederatedGraph.load(
        sources, settings=settings, probe=_cross_ns_probe
    )
    # ... BM25, EmbeddingStore, Embedder 初始化（与 v5.7 相同）...
    return _federated
```

#### 新增 MCP tools

```python
@mcp.tool()
def list_namespaces() -> str:
    """List all loaded knowledge graph namespaces with statistics.

    Returns JSON: {namespaces: [{name, node_count, edge_count,
                                 community_count, hub_set_version}]}
    """

@mcp.tool()
def cross_graph_bridges(
    namespace_a: str,
    namespace_b: str,
) -> str:
    """Return bridge edges between two namespaces.

    Returns JSON: {bridges: [{source_ns, source_id, target_ns, target_id,
                               relation, confidence, weight}]}
    """
```

---

### 2.4 三层 Bridge（federated.py 改动）

**改动文件**：`prism_rag/store/federated.py`

现有的 `_build_embedding_bridges` 做全量 O(N×M) 对比，需替换为三层架构。

#### Layer-2：Hub 代表点选取与骨架 Bridge

```python
import random
import itertools
from functools import lru_cache

def _select_hub_nodes(
    self,
    namespace: str,
    k: int = 30,
    m: int = 20,
) -> list[str]:
    """Select k top-degree + m random nodes as bridge representatives.

    Args:
        namespace: Target namespace key.
        k:         Top-degree nodes count (default 30).
        m:         Random sample supplement count (default 20).

    Returns:
        List of bare node IDs (no namespace prefix). Length <= k + m.

    Notes:
        - Uses degree centrality (O(E)) rather than betweenness (O(VE)).
        - Random supplement ensures low-degree but semantically important
          nodes (e.g., isolated concept nodes) get representation.
    """
    graph = self._graphs.get(namespace)
    if graph is None:
        return []
    degrees = sorted(graph.g.degree(), key=lambda x: -x[1])
    top_k = [n for n, _ in degrees[:k]]
    pool = [n for n, _ in degrees[k:]]
    rand_m = random.sample(pool, min(m, len(pool))) if pool else []
    return top_k + rand_m


def _build_hub_bridges(
    self,
    stores: dict[str, "EmbeddingStore"],
    threshold: float = 0.70,
    top_k: int = 3,
) -> None:
    """Layer-2: build skeleton bridges using hub representative nodes.

    Complexity: O(H_a × H_b) where H = 50 per namespace.
    Maximum: O(50²) = 2500 comparisons per namespace pair.

    For each hub-pair hit (cosine > threshold):
      - Creates community-level bridge edge.
      - community_id="inbox" nodes are skipped (unclustered hotpatch nodes).

    After completion: writes hub_node_ids + hub_set_version to registry_cache.
    """
    if len(self._graphs) < 2 or not stores:
        return

    for ns_a, ns_b in itertools.combinations(sorted(self._graphs.keys()), 2):
        store_a = stores.get(ns_a)
        store_b = stores.get(ns_b)
        if store_a is None or store_b is None:
            continue

        hubs_a = self._select_hub_nodes(ns_a)    # up to 50 nodes
        hubs_b = self._select_hub_nodes(ns_b)    # up to 50 nodes

        for node_id_a in hubs_a:
            # Skip inbox (unclustered) nodes
            node_data_a = self._graphs[ns_a].g.nodes.get(node_id_a, {})
            if node_data_a.get("community_id") == "inbox":
                continue

            vec_a = store_a.get_embedding(node_id_a)
            if vec_a is None:
                continue

            results = store_b.search_subset(vec_a, node_ids=hubs_b, top_k=top_k)
            for node_id_b, distance in results:
                node_data_b = self._graphs[ns_b].g.nodes.get(node_id_b, {})
                if node_data_b.get("community_id") == "inbox":
                    continue

                similarity = 1.0 - (distance / 2.0)
                if similarity < threshold:
                    continue

                self._bridges.append({
                    "source_ns": ns_a,
                    "source_id": node_id_a,
                    "target_ns": ns_b,
                    "target_id": node_id_b,
                    "relation": "hub_similar",
                    "confidence": "INFERRED",
                    "weight": round(similarity, 4),
                    "source_pass": "hub_ann",
                })

    hub_count = sum(1 for b in self._bridges if b["relation"] == "hub_similar")
    logger.info(f"[federated] hub bridges: {hub_count}")
```

#### Layer-3：Lazy Query-time ANN（LRU 缓存）

```python
# NOTE: lru_cache 只能缓存 hashable 参数，embedding 用哈希值传入

@lru_cache(maxsize=256)
def _lazy_cross_ann_cached(
    source_ns: str,
    source_hub_version: int,
    target_ns: str,
    target_hub_version: int,
    query_embedding_hash: str,   # hashlib.sha256(vec.tobytes()).hexdigest()[:16]
    threshold: float,
    top_k: int,
) -> tuple[tuple[str, float], ...]:
    """Layer-3: node-level ANN across namespace boundary.

    Cache key includes hub_set_version — stale entries auto-invalidate
    when hub rebuild increments the version.

    Returns tuple of (node_id, similarity) pairs (tuple for hashability).
    """
    # Implementation delegates to _federated_lazy_ann()
    # which has access to live _embedding_stores
    ...


def cross_ns_query_ann(
    self,
    source_ns: str,
    query_vec: "np.ndarray",
    target_ns: str,
    stores: dict[str, "EmbeddingStore"],
    threshold: float = 0.65,
    top_k: int = 5,
) -> list[tuple[str, float]]:
    """Public API for Layer-3 lazy ANN, called from search_knowledge(scope=all).

    Automatically uses cache; invalidated when hub_set_version changes.
    """
    import hashlib, numpy as np

    src_version = self._hub_versions.get(source_ns, 0)
    tgt_version = self._hub_versions.get(target_ns, 0)
    vec_hash = hashlib.sha256(query_vec.astype(np.float32).tobytes()).hexdigest()[:16]

    return list(_lazy_cross_ann_cached(
        source_ns, src_version,
        target_ns, tgt_version,
        vec_hash, threshold, top_k,
    ))
```

#### 版本号管理

```python
# FederatedGraph 新增字段（__init__）
self._hub_versions: dict[str, int] = {}   # ns → hub_set_version

# 从 registry_cache 加载版本号
def _load_hub_versions(self) -> None:
    from prism_rag.store.registry_cache import get_hub_version
    for ns in self._graphs:
        self._hub_versions[ns] = get_hub_version(ns)

# hub rebuild 完成后更新版本号
def _increment_hub_version(self, namespace: str) -> int:
    new_ver = self._hub_versions.get(namespace, 0) + 1
    self._hub_versions[namespace] = new_ver
    return new_ver
```

#### 改动后的 `build_bridges` 方法

```python
def build_bridges(
    self,
    stores: dict[str, "EmbeddingStore"] | None = None,
    bridge_threshold: float = 0.70,
    bridge_top_k: int = 5,
    probe=None,
) -> int:
    """Build all bridge layers. Replaces v5.7 O(N*M) embedding bridge.

    Layer-1: shared_tag + import/dependency  (deterministic, O(nodes))
    Layer-2: hub ANN skeleton                (O(50²) per ns-pair)
    Layer-3: lazy per-query                  (deferred, not called here)
    """
    self._bridges.clear()
    self._unified = None
    if self._single:
        return 0

    # Layer-1: shared tag bridges（逻辑与 v5.7 相同，保留）
    self._build_tag_bridges()

    # Layer-1b: import/dependency bridges（新增）
    self._build_import_bridges()

    # Layer-2: hub ANN skeleton bridge（替换 v5.7 全量 embedding bridge）
    if stores and len(stores) >= 2:
        self._build_hub_bridges(stores, bridge_threshold, bridge_top_k)

    if probe is not None:
        for bridge in self._bridges:
            probe.on_edge_created(bridge)

    logger.info(f"[federated] built {len(self._bridges)} bridge edges "
                f"({len(self._graphs)} namespaces)")
    return len(self._bridges)
```

---

### 2.5 写操作热补丁（server.py 改动）

**改动文件**：`prism_rag/mcp_server/server.py`

在 `write_note` / `patch_note` 工具执行写操作成功后，立即将新节点注入内存图，并将索引重建放到后台异步队列。

#### 热补丁注入函数

```python
import asyncio
from collections import defaultdict

# 待重建队列：ns → dirty flag
_rebuild_pending: dict[str, bool] = defaultdict(bool)
_rebuild_lock: asyncio.Lock | None = None   # 延迟初始化


async def _hot_patch_node(
    namespace: str,
    node_id: str,
    node_data: dict,
) -> None:
    """Immediately inject a new/updated node into the in-memory graph.

    Step 1: Add node to namespace's KnowledgeGraph in-memory.
    Step 2: Mark community_id = "inbox" (unclustered placeholder).
    Step 3: Invalidate unified_view cache.
    Step 4: Schedule async BM25 + Lance rebuild.

    Does NOT wait for rebuild. Returns immediately.
    """
    fg = _federated
    if fg is None:
        return

    graph = fg.get_graph(namespace)
    if graph is None:
        logger.warning(f"[hot_patch] namespace not loaded: {namespace}")
        return

    # 注入内存图
    graph.g.add_node(node_id, **{**node_data, "community_id": "inbox"})
    fg._unified = None   # 失效统一视图缓存

    # 标记为待重建
    _rebuild_pending[namespace] = True

    # 异步调度重建（非阻塞）
    asyncio.create_task(_async_rebuild(namespace))
    logger.debug(f"[hot_patch] injected {namespace}::{node_id}")


async def _async_rebuild(namespace: str) -> None:
    """Background: rebuild BM25, Lance, Leiden, hub nodes for namespace.

    Coalesces multiple rapid writes: if rebuild is already running for
    this namespace, the new flag will trigger another pass.

    Guaranteed consistency: new nodes queryable via graph traversal
    immediately (hot_patch); BM25/ANN lag up to 30 minutes.
    """
    global _rebuild_lock
    if _rebuild_lock is None:
        _rebuild_lock = asyncio.Lock()

    async with _rebuild_lock:
        if not _rebuild_pending.get(namespace):
            return
        _rebuild_pending[namespace] = False

        await asyncio.sleep(0)   # yield to event loop

        graph = _federated.get_graph(namespace)
        if graph is None:
            return

        try:
            # 1. rebuild BM25
            idx = BM25Index()
            idx.build(graph)
            _bm25_indices[namespace] = idx

            # 2. rebuild Lance (async-friendly 包装)
            await _rebuild_lance(namespace, graph)

            # 3. re-run Leiden clustering
            from prism_rag.cluster.leiden import run_leiden
            await asyncio.get_event_loop().run_in_executor(
                None, run_leiden, graph
            )

            # 4. reselect hub nodes, increment hub_set_version
            hubs = _federated._select_hub_nodes(namespace)
            new_ver = _federated._increment_hub_version(namespace)
            from prism_rag.store.registry_cache import update_hub
            community_count = len(set(
                d.get("community_id", "") for _, d in graph.g.nodes(data=True)
                if d.get("community_id") not in ("", "inbox")
            ))
            update_hub(namespace, hubs, new_ver, community_count)

            # 5. rebuild Layer-2 bridges for affected namespace pairs
            _federated.build_bridges(stores=_embedding_stores)

            logger.info(f"[rebuild] completed for {namespace} (hub_ver={new_ver})")
        except Exception as exc:
            logger.error(f"[rebuild] failed for {namespace}: {exc}", exc_info=True)
            _rebuild_pending[namespace] = True   # 重置 flag，下次重试


async def _scheduled_rebuild() -> None:
    """Periodic 30-minute rebuild sweep for namespaces with pending inbox nodes."""
    while True:
        await asyncio.sleep(1800)
        for ns in (_federated.namespaces if _federated else []):
            graph = _federated.get_graph(ns)
            if graph is None:
                continue
            has_inbox = any(
                d.get("community_id") == "inbox"
                for _, d in graph.g.nodes(data=True)
            )
            if has_inbox:
                _rebuild_pending[ns] = True
                asyncio.create_task(_async_rebuild(ns))
```

#### write_note 改动示意

```python
@mcp.tool()
async def write_note(
    path: str,
    content: str,
    # ... 现有参数 ...
) -> str:
    """Full-write with CAS + atomic + audit. [v7.0: hot-patch on success]"""
    # --- 原有写入逻辑（不变）---
    result = await _do_write_note(path, content, ...)

    # --- v7.0 热补丁（写成功后附加）---
    if result.get("status") == "ok":
        ns = _resolve_namespace(path)
        node_id, node_data = _extract_node(path, content)
        await _hot_patch_node(ns, node_id, node_data)

    return json.dumps(result, ensure_ascii=False)
```

---

### 2.6 联邦元图可视化（visualize.py 改动）

**改动文件**：`prism_rag/report/visualize.py`

#### 函数签名

```python
import math
from pathlib import Path
from typing import Any

def generate_meta_graph_html(
    federated: "FederatedGraph",
    output_path: Path,
    registry_cache: dict[str, Any] | None = None,
) -> None:
    """Generate federation meta-graph HTML.

    Each namespace → one super-node (size = sqrt(node_count) × 5).
    Bridge edges → dashed lines (width = log(bridge_count + 1)).
    Click on super-node → portal link to namespace's graph.html.

    Args:
        federated:      Loaded FederatedGraph instance.
        output_path:    Target HTML file path (e.g. ~/.prismrag/meta_graph.html).
        registry_cache: Optional dict of registry entries for extra metadata
                        (hub_set_version, community_count per namespace).
    """


def generate_query_result_html(
    nodes: list[dict[str, Any]],
    links: list[dict[str, Any]],
    output_path: Path,
    query: str = "",
) -> None:
    """Generate a readonly snapshot HTML for a cross-graph query result.

    Nodes from multiple namespaces colored by namespace.
    Bridge edges shown as dashed lines.
    No editing, no further exploration — snapshot only.

    Args:
        nodes:       List of node dicts with keys: id, label, namespace, kind, community.
        links:       List of link dicts with keys: source, target, relation, _dashed.
        output_path: Target HTML path (e.g. /tmp/prismrag_query_1716720000.html).
        query:       Original query string, shown in HTML title.
    """
```

#### CLI 命令扩展（`cli.py`）

```python
# 新增子命令
@app.command()
def visualize(
    federated: bool = typer.Option(False, "--federated", help="Generate federation meta-graph"),
    namespace: str = typer.Option("", "--namespace", "-n", help="Single namespace graph"),
    output: Path = typer.Option(None, "--output", "-o", help="Output HTML path"),
) -> None:
    """Generate visualization HTML for the knowledge graph.

    Examples:
        prism-rag visualize --federated
        prism-rag visualize --namespace pulsify
    """
```

---

## 3. 数据 Schema

### 3.1 manifest.json（source of truth）

**位置**：`<data_dir>/manifest.json`（即 `.prismrag/<ns>/manifest.json`）

```json
{
  "schema_version": 1,
  "namespace": "pulsify",
  "vault_path": "/home/kingy/Projects/Pulsify",
  "data_dir": "/home/kingy/Projects/Pulsify/.prismrag/pulsify",
  "ingest_time": "2026-05-26T10:30:00Z",
  "node_count": 1240,
  "edge_count": 3820,
  "embedding_model": "ollama/bge-m3",
  "embedding_dim": 1024,
  "prismrag_version": "5.7.0"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `schema_version` | int | 固定为 1，预留升级用 |
| `namespace` | str | 唯一标识符，与目录名一致 |
| `vault_path` | str (abspath) | 被 ingest 的 vault 或 repo 根目录 |
| `data_dir` | str (abspath) | PrismRag 输出目录（graph.json 所在） |
| `ingest_time` | str (ISO-8601 UTC) | 最后一次完整 ingest 的时间 |
| `node_count` | int | ingest 完成后的节点总数 |
| `edge_count` | int | ingest 完成后的边总数 |
| `embedding_model` | str | `{backend}/{model}` 格式，如 `ollama/bge-m3` |
| `embedding_dim` | int | 向量维度，如 1024 |
| `prismrag_version` | str | 写入时的 PrismRag 版本号 |

**不存入 manifest 的字段**（均为派生数据，属于 registry_cache）：
- `hub_node_ids`：由 degree 排序计算，随图变化
- `hub_set_version`：计数器，随 hub 重建递增
- `community_count`：从 graph.json 节点的 `community_id` 字段派生

### 3.2 registry_cache.json（派生数据 + 发现索引）

**位置**：`~/.prismrag/registry_cache.json`

```json
{
  "schema_version": 1,
  "entries": {
    "pulsify": {
      "manifest_path": "/home/kingy/Projects/Pulsify/.prismrag/pulsify/manifest.json",
      "last_seen": "2026-05-26T10:30:00Z",
      "hub_node_ids": [
        "pulsify::AudioEngine",
        "pulsify::TrackManager",
        "pulsify::MixerCore",
        "..."
      ],
      "hub_set_version": 3,
      "community_count": 42
    },
    "nimbus": {
      "manifest_path": "/home/kingy/Foundation/Vault/.prismrag/nimbus/manifest.json",
      "last_seen": "2026-05-26T09:15:00Z",
      "hub_node_ids": [
        "nimbus::ZenithLoom",
        "nimbus::PrismRag",
        "..."
      ],
      "hub_set_version": 1,
      "community_count": 87
    }
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `schema_version` | int | 固定为 1 |
| `entries` | dict[str, Entry] | key = namespace |
| `entries[ns].manifest_path` | str (abspath) | 对应 manifest.json 的绝对路径 |
| `entries[ns].last_seen` | str (ISO-8601) | 最后一次 `register()` 时间 |
| `entries[ns].hub_node_ids` | list[str] | 代表点 node ID 列表（含 ns:: 前缀） |
| `entries[ns].hub_set_version` | int | Hub 集版本号；0 = 从未计算 |
| `entries[ns].community_count` | int | 最新社区数量（Leiden 结果） |

### 3.3 Bridge Edge 格式（统一，含新 Layer-2 类型）

```json
{
  "source_ns": "pulsify",
  "source_id": "AudioEngine",
  "target_ns": "nimbus",
  "target_id": "SignalProcessing",
  "relation": "hub_similar",
  "confidence": "INFERRED",
  "weight": 0.847,
  "source_pass": "hub_ann"
}
```

`relation` 取值：

| 值 | 层级 | 说明 |
|----|------|------|
| `shared_tag` | Layer-1 | 两个 namespace 有相同 #tag |
| `import_dep` | Layer-1 | 代码 import 跨 namespace |
| `hub_similar` | Layer-2 | Hub 代表点 ANN（余弦 > 0.70） |
| `embedding_similar` | Layer-3 | 节点级 lazy ANN（query-time） |

---

## 4. 关键流程序列图

### 4.1 ingest 完成后 manifest 写入 + registry 注册

```
  CLI (prism-rag ingest)
       │
       ├── parse_vault / parse_code
       │        │
       │        └── KnowledgeGraph.build()
       │
       ├── graph.save("graph.json")          ← 现有逻辑
       │
       ├── _write_manifest(out_dir, ns, ...)  ← v7.0 新增
       │        │
       │        ├── build manifest dict
       │        ├── atomic_write(tmp → manifest.json)
       │        └── _register_manifest(manifest_path, ns)
       │                    │
       │                    ├── FileLock(registry_cache.json.lock)
       │                    ├── _load_cache()
       │                    ├── upsert entries[ns]
       │                    └── _atomic_write(registry_cache.json)
       │
       └── print("✓ ingest complete")
```

### 4.2 serve 启动自动发现

```
  prism-rag serve
       │
       └── _ensure_federated()
                │
                ├── [PRISM_GRAPHS 设置?]
                │     ├── YES → use settings.resolved_graphs  (v5.7 兼容路径)
                │     └── NO  → discover_all()                (v7.0 新路径)
                │                    │
                │                    ├── load registry_cache.json
                │                    ├── for each entry: stat(manifest_path)
                │                    ├── evict ghosts (不存在的 manifest)
                │                    └── return live entries
                │
                ├── for each entry:
                │     └── GraphSource(ns, vault_path, data_dir)
                │
                ├── FederatedGraph.load(sources)
                │        │
                │        ├── for each source: KnowledgeGraph.load(graph.json)
                │        ├── for each source: EmbeddingStore.load(lance/)
                │        └── build_bridges()
                │                │
                │                ├── Layer-1: _build_tag_bridges()
                │                ├── Layer-1b: _build_import_bridges()
                │                └── Layer-2: _build_hub_bridges()
                │                          │
                │                          ├── _select_hub_nodes(ns_a)  → 50 nodes
                │                          ├── _select_hub_nodes(ns_b)  → 50 nodes
                │                          ├── store_b.search_subset(vec, hubs_b)
                │                          └── append hub_similar bridges
                │
                ├── build BM25 per namespace
                └── return FederatedGraph
```

### 4.3 MCP write_note 热补丁流程

```
  MCP Client → write_note(path, content)
                    │
                    ├── validate + CAS check
                    ├── atomic_write(vault file)
                    ├── audit_log.append()
                    │
                    ├── [SUCCESS?]
                    │     │
                    │     └── await _hot_patch_node(ns, node_id, node_data)
                    │                │
                    │                ├── graph.g.add_node(node_id, community_id="inbox")
                    │                ├── fg._unified = None  (invalidate cache)
                    │                └── asyncio.create_task(_async_rebuild(ns))
                    │                         │              (非阻塞，立即返回)
                    │                         │
                    │                         └── [后台 coroutine]
                    │                               ├── BM25Index.rebuild(graph)
                    │                               ├── Lance rebuild
                    │                               ├── Leiden re-cluster
                    │                               ├── select_hub_nodes → increment_hub_version
                    │                               ├── update_hub(registry_cache)
                    │                               └── build_bridges() (Layer-2 重建)
                    │
                    └── return {"status": "ok", ...}  ← 毫秒级响应
```

### 4.4 跨图查询的三层 Bridge 决策流程

```
  search_knowledge(query, scope="")
          │
          ├── hybrid_search per namespace  (BM25 + ANN + exact)
          │
          ├── [单 namespace 命中?]
          │     └── YES → bfs_traverse / dfs_traverse → return
          │
          ├── [scope="" AND multi-graph?]
          │     └── YES → cross-ns expansion
          │
          └── cross_ns_expansion(entry_ns, entry_node, query_vec)
                   │
                   ├── [Layer-1 bridge 存在?]
                   │     └── YES → 直接走已有 bridge edges（零额外成本）
                   │
                   ├── [Layer-2 bridge 存在?]
                   │     └── YES → 展开对端 community 节点
                   │
                   └── [Layer-2 miss → Layer-3 lazy ANN]
                              │
                              ├── hash(query_vec) + hub_set_version → cache key
                              ├── lru_cache hit? → return cached
                              └── lru_cache miss:
                                    ├── store_b.search(query_vec, top_k=5)
                                    ├── filter(similarity > 0.65)
                                    ├── update lru_cache
                                    └── return results
```

---

## 5. 错误处理与降级策略

### 5.1 manifest.json 写入失败

**场景**：磁盘满、权限不足、tmp 文件写入中断。

**策略**：
- `_write_manifest` 内部捕获所有异常，记录 WARNING 日志，**不中断 ingest 流程**。
- 幂等：下次 `ingest` 重新写入，不影响已生成的 `graph.json`。
- 没有 manifest 的 namespace 不会被 `discover_all()` 发现，但仍可通过 `PRISM_GRAPHS` 手动配置（向后兼容）。

### 5.2 registry_cache.json 读取失败

**场景**：JSON 损坏、文件锁超时（5 秒）、并发写冲突。

**策略**：
- `_load_cache` 若解析失败返回空 schema `{"schema_version": 1, "entries": {}}`，不抛异常。
- `register()` 的 FileLock 设 `timeout=5`；超时时记录 WARNING 并跳过注册（不阻断 ingest）。
- `discover_all()` 失败时记录 ERROR 并返回空列表；serve 继续启动但联邦图为空，MCP tools 降级为单图模式（若 PRISM_GRAPHS 有值则使用该值）。

### 5.3 hub ANN 构建失败（Layer-2）

**场景**：EmbeddingStore 向量维度不匹配、Lance 数据库损坏。

**策略**：
- `_build_hub_bridges` 对每个 namespace pair 的 ANN 调用包 `try/except`，失败则跳过该 pair 并记录 WARNING。
- 降级到仅 Layer-1（shared_tag bridge），联邦图仍可用，只是跨图语义关联较弱。
- 在 `list_namespaces()` 返回中标注 `"hub_bridge_status": "unavailable"` 让 LLM 感知状态。

### 5.4 热补丁注入失败

**场景**：内存图锁定、node_id 格式错误。

**策略**：
- `_hot_patch_node` 失败不影响已完成的物理写入（vault 文件已落盘）。
- 记录 WARNING；下次 `serve` 重启时全量重载，包含最新 `graph.json`。
- 后台 `_async_rebuild` 失败时将 `_rebuild_pending[ns] = True` 重置，由 30 分钟定时任务补救。

### 5.5 registry_cache 幽灵条目（ghost entries）

**场景**：vault 被删除、data_dir 被清空，但 registry_cache 中仍有对应条目。

**策略**：
- `discover_all()` 对每条 `manifest_path` 做 `Path.exists()` 探活（< 1ms）。
- 幽灵条目被静默清除并重写 registry_cache。
- 日志级别：`DEBUG`，不产生 WARNING（正常运维场景）。

### 5.6 filelock 在 WSL 上的注意事项

- 使用 `filelock` 库（基于 `os.O_EXCL` + 轮询），而非 `fcntl.flock`（WSL 不完整实现）。
- lock 文件路径：`{cache_path}.lock`（与 cache 同目录）。
- timeout=5 秒防死锁；超时后以 `try/except FileLockTimeout` 记录 WARNING 并继续。

---

## 6. 迁移路径（v5.7 → v7.0）

### 6.1 向后兼容保证

v7.0 **不破坏任何现有 v5.7 配置**：

| v5.7 行为 | v7.0 变化 |
|-----------|----------|
| `PRISM_GRAPHS` 环境变量配置多图 | 完全保留；若设置则跳过 `discover_all()` |
| 单图模式（`PRISM_VAULT_PATH` + `PRISM_DATA_DIR`）| 完全保留 |
| MCP tools 的现有参数 | 不变；`scope` 参数已在 v5.7 添加 |
| `graph.json` / `lance/` 目录结构 | 不变 |
| `KNOW-ID` 分配器（`store/registry.py`） | 与 `registry_cache.py` 完全独立，无冲突 |

### 6.2 升级步骤（v5.7 用户）

```bash
# Step 1: 升级 package（包含 filelock 依赖）
pip install "prism-rag[federation]>=6.0"

# Step 2: 对现有 namespace 补写 manifest.json
# 选项 A：重新 ingest（推荐，更新节点数等统计）
prism-rag ingest --namespace pulsify

# 选项 B：手动写 manifest（跳过重新 ingest，仅注册发现）
prism-rag manifest --namespace pulsify --data-dir .prismrag/pulsify/

# Step 3: 验证注册
prism-rag list-namespaces
# 输出: pulsify (1240 nodes, 42 communities)

# Step 4: 可选 - 移除 PRISM_GRAPHS 环境变量（切换到自动发现）
# unset PRISM_GRAPHS  ← 之后 serve 自动用 registry_cache
```

### 6.3 manifest 补写工具（新 CLI 子命令）

```python
@app.command()
def manifest(
    namespace: str = typer.Argument(..., help="Namespace name"),
    data_dir: Path = typer.Option(..., "--data-dir", help="Path to .prismrag/<ns>/ dir"),
) -> None:
    """Register an existing ingest output without re-running full ingest.

    Reads graph.json to get node/edge counts. Writes manifest.json and
    registers with registry_cache. For upgrading v5.7 → v7.0.
    """
```

### 6.4 hub 节点集冷启动

- v5.7 升级后，registry_cache 中的 `hub_node_ids` 为空列表，`hub_set_version = 0`。
- Layer-2 bridge 不可用，自动降级到 Layer-1（shared_tag）。
- 第一次 `serve` 启动后，`_build_hub_bridges` 会计算并写入 hub 节点集（版本号变为 1）。
- 无需人工干预。

---

## 7. 实现顺序 P0 → P3

### P0：最小联邦发现（~2-3 天工作量）

**目标**：ingest 后 manifest 自动写入，serve 自动发现，无需 PRISM_GRAPHS。

| 工作项 | 类型 | 文件 | 行数估算 |
|--------|------|------|---------|
| P0.1 `_write_manifest()` 函数 | 修改 | `prism_rag/cli.py` | ~50 行 |
| P0.2 各 ingest 命令末尾调用 `_write_manifest` | 修改 | `prism_rag/cli.py` | ~15 行 |
| P0.3 `registry_cache.py`：`register` + `discover_all` + `get_manifest` | 新建 | `prism_rag/store/registry_cache.py` | ~120 行 |
| P0.4 `_ensure_federated` 改造：优先 registry_cache | 修改 | `prism_rag/mcp_server/server.py` | ~40 行 |
| P0.5 新增 `list_namespaces` MCP tool | 修改 | `prism_rag/mcp_server/server.py` | ~25 行 |
| P0.6 `manifest` CLI 子命令（升级工具） | 修改 | `prism_rag/cli.py` | ~30 行 |
| P0.7 `filelock` 加入 `pyproject.toml` | 修改 | `pyproject.toml` | ~3 行 |
| P0.8 单元测试 | 新建 | `tests/test_federation_p0.py` | ~120 行 |

**P0 完成标志**：`prism-rag ingest → list-namespaces` 能看到新 namespace，无需设置 `PRISM_GRAPHS`。

---

### P1：Hub ANN Bridge + LRU 缓存（~3-4 天工作量）

**目标**：O(50²) hub 代表点 bridge 替换 O(N×M) 全量 embedding bridge。

**依赖**：P0 完成。

| 工作项 | 类型 | 文件 | 行数估算 |
|--------|------|------|---------|
| P1.1 `_select_hub_nodes()` 方法 | 修改 | `prism_rag/store/federated.py` | ~30 行 |
| P1.2 `_build_hub_bridges()` 方法 | 修改 | `prism_rag/store/federated.py` | ~80 行 |
| P1.3 删除旧 `_build_embedding_bridges`（或保留为 Layer-3 fallback） | 修改 | `prism_rag/store/federated.py` | ~(-60) 行 |
| P1.4 `build_bridges()` 路由三层 | 修改 | `prism_rag/store/federated.py` | ~20 行 |
| P1.5 `_hub_versions` 字段 + `_increment_hub_version` | 修改 | `prism_rag/store/federated.py` | ~20 行 |
| P1.6 `cross_ns_query_ann()` + `_lazy_cross_ann_cached()` | 修改 | `prism_rag/store/federated.py` | ~60 行 |
| P1.7 `registry_cache.update_hub()` 方法 | 修改 | `prism_rag/store/registry_cache.py` | ~30 行 |
| P1.8 `EmbeddingStore.search_subset()` 方法（仅搜指定 node_ids） | 修改 | `prism_rag/store/embedding_store.py` | ~40 行 |
| P1.9 `cross_graph_bridges` MCP tool | 修改 | `prism_rag/mcp_server/server.py` | ~25 行 |
| P1.10 单元测试 | 新建 | `tests/test_hub_bridge.py` | ~150 行 |

**P1 完成标志**：两个有 50+ 节点的 namespace，serve 启动 bridge 构建 < 2 秒。

---

### P2：写操作热补丁 + 后台 Rebuild（~2-3 天工作量）

**目标**：write_note 后新节点立即可被图遍历查到，BM25/ANN 最终一致。

**依赖**：P1 完成。

| 工作项 | 类型 | 文件 | 行数估算 |
|--------|------|------|---------|
| P2.1 `_hot_patch_node()` async 函数 | 修改 | `prism_rag/mcp_server/server.py` | ~40 行 |
| P2.2 `_async_rebuild()` async 函数 | 修改 | `prism_rag/mcp_server/server.py` | ~80 行 |
| P2.3 `_scheduled_rebuild()` 30 分钟定时任务 | 修改 | `prism_rag/mcp_server/server.py` | ~30 行 |
| P2.4 `write_note` / `patch_note` tool 末尾调用 `_hot_patch_node` | 修改 | `prism_rag/mcp_server/server.py` | ~20 行 |
| P2.5 serve startup 启动 `_scheduled_rebuild` 后台任务 | 修改 | `prism_rag/mcp_server/server.py` | ~10 行 |
| P2.6 集成测试 | 新建 | `tests/test_hot_patch.py` | ~80 行 |

**P2 完成标志**：`write_note("test.md", ...)` 后立即调用 `explain_node("test")` 可见节点；BM25 搜索在 30 分钟内追上。

---

### P3：联邦元图可视化（~2-3 天工作量）

**目标**：元图 HTML + 跨图查询结果动态 HTML。

**依赖**：P0 完成（P1/P2 可并行）。

| 工作项 | 类型 | 文件 | 行数估算 |
|--------|------|------|---------|
| P3.1 `generate_meta_graph_html()` 函数 | 修改 | `prism_rag/report/visualize.py` | ~100 行 |
| P3.2 `generate_query_result_html()` 函数 | 修改 | `prism_rag/report/visualize.py` | ~80 行 |
| P3.3 `visualize --federated` CLI 命令 | 修改 | `prism_rag/cli.py` | ~30 行 |
| P3.4 `search_knowledge(scope="all", visualize=True)` 可选 HTML 输出 | 修改 | `prism_rag/mcp_server/server.py` | ~20 行 |
| P3.5 元图 HTML 模板（namespace 超级节点样式） | 修改 | `prism_rag/report/visualize.py` | ~60 行 |
| P3.6 可视化测试（无浏览器，检查 HTML 结构） | 新建 | `tests/test_visualize_federated.py` | ~60 行 |

**P3 完成标志**：`prism-rag visualize --federated` 生成 `~/.prismrag/meta_graph.html`，浏览器可打开，点击 namespace 超级节点跳转到对应 `graph.html`。

---

## 8. 测试矩阵

### P0 验收标准

| 测试用例 | 方法 | 预期结果 |
|---------|------|---------|
| manifest 写入 | `prism-rag ingest` 后检查文件 | `manifest.json` 存在，字段完整，JSON 可解析 |
| manifest 原子性 | 模拟写入中途 kill | 不存在半写文件；下次 ingest 重写 |
| registry 注册 | `register(manifest_path, ns)` | `registry_cache.json` 中存在对应条目 |
| 幽灵清理 | 注册两个 ns，删除一个 vault，`discover_all()` | 只返回 1 个有效条目 |
| serve 自动发现 | 无 `PRISM_GRAPHS`，`serve` 启动 | 从 registry_cache 加载正确 namespace 数量 |
| PRISM_GRAPHS 兼容 | 设置 `PRISM_GRAPHS`，`serve` 启动 | 使用显式配置，不查 registry_cache |
| `list_namespaces` tool | 两个 namespace 加载后调用 | 返回包含两个 ns 的 JSON，node_count 准确 |
| scope 过滤 | `search_knowledge("x", scope="ns1")` | 结果只含 ns1 节点 |
| scope="" 全图搜索 | `search_knowledge("x", scope="")` | 结果可能来自任意 ns |
| 并发注册 | 10 个线程同时 `register()` | registry_cache 无损坏，所有 ns 都在 |

### P1 验收标准

| 测试用例 | 方法 | 预期结果 |
|---------|------|---------|
| hub 选取 | `_select_hub_nodes("ns", k=30, m=20)` | 返回 <= 50 个 node_id，top-30 是度最高的 |
| hub 选取（小图） | 图仅 20 节点 | 返回所有 20 节点，不越界 |
| hub bridge 复杂度 | 两个各 1000 节点的 ns | bridge 构建时间 < 2 秒 |
| hub bridge 质量 | 有明确相关 topic 的两个 ns | 至少 1 条 hub_similar bridge 存在 |
| LRU 缓存命中 | 相同 query 调用两次 `cross_ns_query_ann` | 第二次不调用 embedding_store.search |
| 版本号失效 | hub rebuild 后缓存 key 变化 | 旧缓存不再命中，重新计算 |
| hub 版本写入 | `_build_hub_bridges` 完成后 | registry_cache 的 `hub_set_version` 递增 |

### P2 验收标准

| 测试用例 | 方法 | 预期结果 |
|---------|------|---------|
| 热补丁立即可见 | `write_note` → 立即 `explain_node` | 节点存在，`community_id="inbox"` |
| 热补丁不阻塞 | 测量 `write_note` 响应时间 | < 100ms（不等待 BM25/Lance 重建） |
| 重建完成后 BM25 可搜 | `write_note` → sleep(rebuild_time) → `search_knowledge` | 新节点被 BM25 搜索命中 |
| 并发写入 | 10 个并发 `write_note` | 所有节点注入成功，rebuild 任务合并（不并发 10 次） |
| 30 分钟定时 | inbox 节点存在超过定时间隔 | `_scheduled_rebuild` 触发，inbox 节点被聚类 |
| rebuild 失败重试 | 模拟 Leiden 抛异常 | `_rebuild_pending[ns]` 重置为 True，下次定时触发重试 |

### P3 验收标准

| 测试用例 | 方法 | 预期结果 |
|---------|------|---------|
| 元图 HTML 生成 | `prism-rag visualize --federated` | `~/.prismrag/meta_graph.html` 存在，HTML 结构有效 |
| 超级节点数量 | 3 个 namespace，检查 HTML 中节点数据 | graphData.nodes 包含 3 个 `kind=namespace` 节点 |
| portal 链接 | 检查 HTML 中超级节点的 `portal_href` | 指向 `<ns>/graph.html`（相对路径） |
| bridge 虚线 | 检查 HTML 中 bridge edge 的 `_dashed` 字段 | 值为 true |
| 查询结果 HTML | `search_knowledge(scope="all", visualize=True)` | 返回内容包含 `[SEND_FILE: /tmp/prismrag_query_*.html]` |
| 无 namespace 时 | `prism-rag visualize --federated`（无已注册 ns） | 输出友好错误，不 crash |

---

## 9. 架构决策记录（ADR）— 2026-06-10 辩论定案

> 经 4 轮 Claude×Gemini 对抗辩论（debate_design）定案。
> 辩论基准为 `FEDERATED_GRAPH_DESIGN.md`（2026-04-13 版）；本节将结论与
> 本文档（2026-05-26 三层 bridge 设计）做对账，**冲突处以本节为准**。

### ADR-1：Bridge 缓存键 = 排序矩阵哈希 + manifest 溯源

**决策**：缓存失效键 = per-namespace embedding 矩阵的 SHA256，矩阵**必须按
node-id 字典序排序后再 `tobytes()`**——否则 ingest 目录遍历顺序变化会导致
内容未变但哈希突变。

**对账**：本文档 2.4 节的 `hub_set_version` 原为递增计数器，**改为基于内容
哈希**：`hub_set_version = sha256(sorted_hub_embedding_matrix)`。
`bridge_manifest.json`（并入 registry_cache.json 或独立文件）记录
`{(ns_a, ns_b): {file, hash_a, hash_b, changed_nodes_a}}`，serve 启动时
通过 manifest 精确溯源上一个有效缓存，而非靠文件名猜测。

**成本**：1499×1536 float32 ≈ 9.2MB → SHA256 < 20ms；4444 节点 < 60ms。可忽略。

### ADR-2：增量 bridge 更新 — 条件性 patch + Dict 缓存结构

**决策**：
- 单 vault 重 ingest 后：变更节点 ≤50 走 patch，>50 走全量重算。
- Bridge 缓存数据结构用 **`Dict[str, List[Tuple[str, float]]]`**
  （key=source_node_id）而非扁平边集 `[(u,v,w)]`——patch 时按 changed_nodes
  直接 pop 对应 key（O(1)），重算该节点新边后 merge 回字典。
  扁平边集的 O(E) 遍历删除是被辩论击穿的反模式。
- Ingest 完成后输出 `changed_nodes` 清单写入 manifest。

**对账**：适用于 Layer-2 hub bridge 缓存；Layer-3 lazy ANN 走 LRU 不受影响。

### ADR-3：跨图 Token Budget — 全局优先队列 + 源图 30% 保底

**决策**：废弃 70/30 硬分配和乘法衰减公式（反例：源图 0.80 vs 跨图 0.99，
`0.99×0.8=0.792 < 0.80` 会错误拒绝高相关跨图转移）。最终设计：

- 源图保底 budget = 总量 × 30%（保留查询上下文锚点，不可转移）。
- 剩余 70% 进**全局 max-heap**：所有 namespace 的候选节点按 relevance
  统一排序，贪心消耗直到 budget 耗尽。

**对账**：本文档原先未定义跨图 budget 分配方案，本条为新增规格，
落点在 `retrieve/` 跨图 BFS 路径（P1 实现范围）。

### ADR-4：Meta-graph 超级节点边权 — 三级加权 + Top-K 截断

**决策**：
- 超级节点间边权 = `wikilink数×10 + tag数×3 + embedding数×1`。
- Embedding bridge 计入边权的条件：similarity ≥ **0.85**（高于查询用的
  0.70 阈值）且每对 namespace **Top-50 截断**，防止大 vault 间边权膨胀。

**对账**：补充 2.6 节 `generate_meta_graph_html` 的边权规格
（原设计仅有 `width = log(bridge_count + 1)`，现明确 bridge_count 的
加权计数法）。

### ADR-5：砍掉 Merged 模式 → v7.1+

**决策**：v7.0 只做 federated。根本理由：v7.0 在 federated 上投入节点级
增量 patch 的精细化设计（ADR-2），而 merged 模式要求全图重 Leiden——
同一版本不可能既追求节点级精度又容忍全图级粗暴重算。
Merged 留给 v7.1+ 作为**离线批处理**功能（如确有跨 vault 社区需求）。

**对账**：本文档本就未含 merged 模式，与 `FEDERATED_GRAPH_DESIGN.md` 的
`multi_graph_mode: "merged"` 配置项冲突——该配置项 v7.0 不实现。

### 辩论中被击穿的五个细节（实现时的红线）

1. node-id 列表哈希会遗漏节点内容变更 → 必须哈希 embedding 矩阵本体
2. 文件名嵌哈希无法溯源旧缓存（祖父悖论）→ 必须 manifest 显式记录
3. 矩阵未排序导致非确定性哈希 → 必须按 node-id 排序
4. 乘法衰减 budget 公式数学失效 → 全局优先队列
5. 扁平边集 O(E) 删除灾难 → Dict[node_id → edges]

---

*文档撰写：PrismRag 技术架构师（Hani）· 2026-05-26*  
*ADR 章节：4 轮对抗辩论定案 · 2026-06-10*  
*版本追踪：参见 `/home/kingy/Foundation/PrismRag/docs/v7.0-implementation-plan.md`*
